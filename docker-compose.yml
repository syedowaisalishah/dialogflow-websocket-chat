services:
  backend:
    build: ./server
    ports:
      - "3001:3001"
    env_file:
      - ./server/.env
    volumes:
      - ./server/src:/app/src
      - ./server/.env:/app/.env
      - ./chat-bot-service.json:/app/chat-bot-service.json:ro
    restart: always
