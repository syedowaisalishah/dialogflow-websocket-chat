services:
  backend:
    build: ./server
    ports:
      - "3001:3001"
    env_file:
      - ./server/.env
    volumes:
      - ./server/src:/app/src
      - ./your-service-account.json:/app/your-service-account.json:ro
    restart: always
