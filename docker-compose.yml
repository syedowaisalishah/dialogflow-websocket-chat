services:
  backend:
    build: ./server
    ports:
      - "3001:3001"
    env_file:
      - ./server/.env
    volumes:
      - ./server/src:/app/src
      - ./server/.env:/app/.env
      - ./server/service-account.json:/app/service-account.json:ro
    restart: always
